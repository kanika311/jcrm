# Legacy Express backend

The app no longer uses a separate Express server. Auth and other APIs live in the Next.js `app/api` routes at the repo root.

This folder keeps the old Express files for reference only; do not run them for production or Vercel.
